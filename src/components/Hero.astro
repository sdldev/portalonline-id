---
const stats = [
    { value: "30+", label: "Portal Aktif" },
    { value: "95+", label: "Score SEO" },
    { value: "5+", label: "Custom Theme" },
    { value: "24/7", label: "Dukungan Tim" },
];
---

<section class="hero">
    <div class="container hero-content">
        <!-- Badge -->
        <div class="animate-slide-up">
            <span class="badge">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                >
                    <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"
                    ></polygon>
                </svg>
                SaaS Platform Portal Online
            </span>
        </div>

        <!-- Headline -->
        <h1 class="hero-title font-display animate-slide-up delay-100">
            Bangun <span class="text-gradient text-glow">Portal online</span>
        </h1>
        <h2 class="hero-title font-display animate-slide-up delay-100">
            Aman, Cepat dan Mudah
        </h2>
        <!-- Subtitle -->
        <p class="hero-subtitle animate-slide-up delay-200">
            Solusi lengkap untuk perusahaan, Media, Portal Berita, dan
            Wartawan Profesional. Platform siap pakai dengan fitur CMS canggih,
            simple dan mudah digunakan real-time, dan Powerfull SEO yang
            membantu Anda fokus pada Bisnis Anda.
        </p>

        <!-- CTA Buttons -->
        <div class="hero-actions animate-slide-up delay-300">
            <a href="/pricing" class="btn btn-primary">
                Mulai Sekarang
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="18"
                    height="18"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <path d="M5 12h14"></path>
                    <path d="m12 5 7 7-7 7"></path>
                </svg>
            </a>
            <a href="/themes" class="btn btn-secondary">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="16"
                    height="16"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                >
                    <polygon points="6 3 20 12 6 21 6 3"></polygon>
                </svg>
                Lihat Tema
            </a>
        </div>

        <!-- Stats Bar -->
        <div class="stats-bar glass animate-slide-up delay-400">
            {
                stats.map((stat) => (
                    <div class="stat-item">
                        <span class="stat-value font-display">
                            {stat.value}
                        </span>
                        <span class="stat-label">{stat.label}</span>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        padding: 8rem 0 4rem;
        overflow: hidden;
    }

    .hero-content {
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
        position: relative;
        z-index: 10;
    }

    .hero-title {
        font-size: clamp(2.5rem, 8vw, 5rem);
        font-weight: 800;
        line-height: 1.1;
        letter-spacing: -0.02em;
    }

    .hero-subtitle {
        max-width: 600px;
        font-size: 1.125rem;
        color: var(--color-text-muted);
        line-height: 1.7;
    }

    .hero-actions {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        width: 100%;
        max-width: 400px;
        margin-top: 0.5rem;
    }

    @media (min-width: 640px) {
        .hero-actions {
            flex-direction: row;
            justify-content: center;
            max-width: none;
        }
    }

    .stats-bar {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
        padding: 1.5rem;
        border-radius: 1.5rem;
        margin-top: 2rem;
        width: 100%;
        max-width: 800px;
    }

    @media (min-width: 640px) {
        .stats-bar {
            grid-template-columns: repeat(4, 1fr);
            padding: 2rem;
            gap: 2rem;
        }
    }

    .stat-item {
        text-align: center;
    }

    .stat-value {
        display: block;
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--color-text);
        margin-bottom: 0.25rem;
    }

    @media (min-width: 640px) {
        .stat-value {
            font-size: 1.75rem;
        }
    }

    .stat-label {
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--color-text-subtle);
        text-transform: uppercase;
        letter-spacing: 0.1em;
    }

    /* Floating decorations */
    .hero-decoration {
        position: absolute;
        inset: 0;
        pointer-events: none;
        overflow: hidden;
    }

    .floating-card {
        position: absolute;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 60px;
        height: 60px;
        border-radius: 1rem;
        color: rgb(var(--color-primary));
    }

    .floating-card:nth-child(1) {
        top: 20%;
        left: 5%;
    }

    .floating-card:nth-child(2) {
        top: 30%;
        right: 8%;
    }

    .floating-card:nth-child(3) {
        bottom: 25%;
        left: 10%;
    }

    @media (max-width: 768px) {
        .floating-card {
            display: none;
        }
    }
</style>
